{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run GLFW",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/cloud_sim.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
